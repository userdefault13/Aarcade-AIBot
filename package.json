{
  "name": "aarcade-aibot",
  "version": "1.0.0",
  "description": "Department Assistant chatbot - standalone app for Cloudflare Pages",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "pages:dev": "npm run build && wrangler pages dev dist",
    "pages:deploy": "npm run build && wrangler pages deploy dist"
  },
  "dependencies": {
    "mathjs": "^15.1.1",
    "vue": "^3.2.45"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^4.0.0",
    "vite": "^4.1.0",
    "wrangler": "^4.65.0"
  }
}
